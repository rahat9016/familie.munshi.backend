server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/familie_munshi_db
    username: postgres
    password: 9016
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true